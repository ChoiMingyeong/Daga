@page "/datatable"

<PageTitle>Datatable</PageTitle>

<MudStack Row="true">
    <MudDataGrid T="DagaDataTable" Items="@(DataTableSvc.DataTables)">
        <ToolBarContent>
            @* <MudTextField @bind-Value="tableSearchString" Placeholder="Search" Adornment="Adornment.Start" Immediate="true"
            AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0"/> *@
        </ToolBarContent>
        <Columns>
            <SelectColumn T="DagaDataTable"/>
            <PropertyColumn Property="x=>x.TableName" />
        </Columns>
    </MudDataGrid>
    @if (null != DataTableSvc.SelectedDataTable)
    {
        <MudDataGrid T="System.Data.DataColumn" Items="DataTableSvc.SelectedDataTable.DataColumns">
            <Columns>
                <PropertyColumn Property="x=>x.ColumnName"/>
            </Columns>
        </MudDataGrid>
    }
</MudStack>

@code {
    string tableSearchString = string.Empty;
}
